plugins {
    id 'java'
}

allprojects {
    group 'cn.codethink'
    version '1.1.0-SNAPSHOT'
}

ext {
    junitVersion = '5.9.2'
    publishScriptFile = new File(project.rootDir, 'publish.gradle')
}